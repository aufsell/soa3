# Defaults for local/dev.
# Can be overridden via JVM system properties (e.g. -Dsoap.host=payara-1) or Mule property sources.

http.port=7777

# In Docker Compose the SOAP backend is reachable by service name on the internal container port.
soap.host=payara-1
soap.port=8080
